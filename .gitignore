/target

# Secrets — jamais commités
.env

# Données personnelles — rester privé (utiliser les fichiers .example)
config/profile.toml
OBJECTIVES.md

# Base de données locale (recréée par Kairos)
data/kairos.db
data/*.db

# Briefings générés (output, pas source)
briefings/

# Logs
*.log
logs/

# Notes personnelles (non publiées)
private/

# Secrets du déploiement Docker (local, non versionné)
deploy/docker/kairos.env

# Notes internes recalibration (gardées hors repo, dans private/)
docs/recalibration/

# Instructions agents (hors du repo public — synchronisées via un repo privé dédié)
CLAUDE.md
